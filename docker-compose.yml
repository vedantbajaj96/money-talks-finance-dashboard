services:
  moneytalks:
    image: vedantbajaj96/moneytalks:latest
    ports:
      - "8502:8502"
    volumes:
      - moneytalks_data:/app/data
      - /var/run/docker.sock:/var/run/docker.sock
    restart: unless-stopped

volumes:
  moneytalks_data:
